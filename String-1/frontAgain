public boolean frontAgain(String str) {
  int length = str.length();
  if (length < 2) {
    return false;
  }
  else if (str.substring(0,2).equals(str.substring(length-2,length))) {
    return true;
  } 
  return false;
}
